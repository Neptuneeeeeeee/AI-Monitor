# Public build profile

The bundle ID, display name, data directory, helper namespace, executable and initial provider selection are checked by the build. Do not change identity for a display-only rename. No provider is enabled on first launch. Entitlements.plist is empty; expanding public entitlements requires deliberate review and corresponding gate changes.
